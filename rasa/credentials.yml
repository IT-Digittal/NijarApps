# NOTA: credentials.yml NO admite la clave `version` — Rasa interpreta cada
# clave de primer nivel como un canal de entrada y aborta el arranque con
# "Failed to find input channel class for 'version'".

# Solo habilitamos la REST API y la HTTP API. La plataforma DTI llama a
# `POST /webhooks/rest/webhook` desde el adapter del chatbot.

rest:

# El servidor HTTP también expone `POST /model/parse` (NLU directo) que
# usamos para obtener intent + confianza desde el adapter.
